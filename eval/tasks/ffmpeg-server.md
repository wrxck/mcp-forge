Create an MCP server that wraps the `ffmpeg` CLI tool. The server should expose tools for: getting media info (via ffprobe), converting formats, extracting audio, generating thumbnails, and trimming clips. All file path parameters must be validated for directory traversal. Include input validation, command injection prevention, and rate limiting. Generate a complete TypeScript project with tests and a Dockerfile.
